import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:test/profile_page.dart';

void main() {
  testWidgets('profile page ...', (tester) async {
    /*
    await tester.pumpWidget(
        const ProviderScope(child: MaterialApp(home: ProfilePage())));

    final userIconFinder = find.byType(CircleAvatar);
    final emailFinder = find.byKey(const ValueKey('emailKey'));
    //final nameFinder = find.byKey(const ValueKey('nameKey'));
    final logoutButtonFinder = find.widgetWithText(ElevatedButton, 'ログアウト');

    expect(userIconFinder, findsOneWidget);
    expect(emailFinder, findsOneWidget);
    //expect(nameFinder, findsOneWidget);
    expect(logoutButtonFinder, findsOneWidget);
    */
  });
}
